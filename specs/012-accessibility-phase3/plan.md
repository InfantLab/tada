# Implementation Plan: Accessibility Phase 3 — WCAG 2.2 AA

**Branch**: `012-accessibility-phase3` | **Date**: 2026-03-29 | **Spec**: [spec.md](spec.md)
**Input**: Feature specification from `/specs/012-accessibility-phase3/spec.md`

## Summary

Implement the remaining 7 items from the WCAG 2.2 AA accessibility audit (Phase 3). This is a frontend-only effort touching Vue components, composables, and utility files. No database changes, no API changes, no new dependencies. The work spans focus management, colour accessibility, keyboard navigation, motion preferences, ARIA semantics, and screen reader announcements.

## Technical Context

**Language/Version**: TypeScript 5.9.3, Vue 3 (Nuxt 4.4.2)
**Primary Dependencies**: Tailwind CSS 3.4.17, vue-virtual-scroller, emoji-picker-element
**Storage**: N/A (no data model changes)
**Testing**: Vitest 3.0.0 (unit), Playwright 1.58.2 (e2e)
**Target Platform**: Web (desktop + mobile browsers)
**Project Type**: Web application (Nuxt 4 fullstack, but this feature is frontend-only)
**Performance Goals**: No performance regression — focus trap listeners and aria-live updates must not cause layout thrash or jank
**Constraints**: All changes must be CSS/HTML/JS only — no new npm dependencies. Must work in both light and dark modes.
**Scale/Scope**: 13 Vue files modified, 2 new composables, 1 utility file updated

## Constitution Check

*No constitution file found — no gates to evaluate.*

## Project Structure

### Documentation (this feature)

```text
specs/012-accessibility-phase3/
├── plan.md              # This file
├── research.md          # Phase 0: colour palettes, focus trap patterns
├── quickstart.md        # Phase 1: implementation guide
└── tasks.md             # Phase 2: generated by /speckit-tasks
```

### Source Code (repository root)

```text
app/
├── composables/
│   ├── useFocusTrap.ts          # NEW — reusable focus trap
│   └── useReducedMotion.ts      # NEW — reactive prefers-reduced-motion
├── components/
│   ├── QuickEntryModal.vue      # MODIFY — apply focus trap
│   ├── EmojiPicker.vue          # MODIFY — apply focus trap
│   ├── RhythmCreateModal.vue    # MODIFY — apply focus trap
│   ├── ContextualHelpPanel.vue  # MODIFY — apply focus trap
│   ├── QuickAddMenu.vue         # MODIFY — apply focus trap
│   ├── CelebrationOverlay.vue   # MODIFY — reduced motion
│   ├── RhythmMonthCalendar.vue  # MODIFY — colourblind palette, legend, titles
│   ├── RhythmYearTracker.vue    # MODIFY — colourblind palette, keyboard nav, legend, titles
│   ├── RhythmBarChart.vue       # MODIFY — bar contrast cue, reduced motion
│   ├── VirtualTimeline.vue      # MODIFY — aria-live filter announcements
│   ├── onboarding/
│   │   └── WelcomeOverlay.vue   # MODIFY — focus trap, reduced motion
│   ├── timeline/
│   │   └── TimelineStrip.vue    # MODIFY — aria-labels, min opacity
│   └── settings/
│       └── WeeklyRhythmsSettings.vue  # MODIFY — toggle switch ARIA
├── pages/
│   └── settings.vue             # MODIFY — toggle switch ARIA
└── utils/
    └── categoryDefaults.ts      # MODIFY — 4 colour values
```

**Structure Decision**: All changes are within the existing `app/` Nuxt project. No new directories needed beyond the two new composable files.

## Complexity Tracking

No constitution violations — table not applicable.
